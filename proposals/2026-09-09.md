# Group 2 Daily Proposal — 2026-09-09

## Summary

No trade proposal today. Instead I'm flagging something that looks more
consequential than any single trade: yesterday's `pod_stock` scanner placed
a real, filled **$100,267 TSLA buy** (279 shares @ $359.38), sized at
roughly 10% of what looks like a ~$990k real portfolio value — and that
single position appears to have consumed almost all of this account's
buying power ever since, silently starving today's otherwise-real scalping
signals of any capital to act on. This needs a human decision (is the
account really that size, and is a single ~$100k position intended?)
before I can respond to anything meaningfully differently tomorrow.

---

## Finding: a ~$100k pod_stock TSLA position is eating all the account's buying power

**What I checked** (all read-only: `bot.log`, `signals.db`, `risk_rules.py`,
`portfolio_manager.py`, `strategy_manager.py` — no live state touched, and
I deliberately did NOT call the IBKR account-balance/positions tools
available in this session, since [[project_single_ib_login_constraint]]
says this account only supports one login and I didn't want to risk
knocking the live bot off its own Gateway session just to check a number):

- `bot.log:206662-663` (2026-09-08 14:18:08 ET):
  `portfolio_manager - INFO - TSLA (STOCK): $99,056.07 (category budget
  $776,997.50 / 1 candidates, capped_by=position_ceiling)` immediately
  followed by `strategy_manager - INFO - Placed buy order for 279.0000x
  TSLA @ $354.08`, which filled seconds later at `$359.38` (`bot.log:
  206732`) — a real fill of **$100,267.02**.
- `portfolio_manager.py`'s pod-stock sizing uses `position_ceiling =
  portfolio_value * MAX_POSITION_PCT_OF_PORTFOLIO` (0.10, `risk_rules.py`
  matches `config.MAX_POSITION_SIZE`). A $99,056 position ceiling implies
  `portfolio_value ≈ $990,560` at that moment — suspiciously close to the
  $1,000,000 default balance IB paper accounts commonly start with.
- Same-cycle `portfolio_manager` log lines show the ETF pod evaluating
  candidates (VWCE, BOVA11, ES3) against a *category budget* of
  **$776,997.50** — consistent with a ~$990k portfolio split across 5 pod
  categories — though I found no matching "Placed buy order" line for any
  of them, so as far as I can tell only the one TSLA fill actually went
  through.
- This one position has a real bracket stop/target attached (`STOCK`
  category uses `config.STOP_LOSS_PERCENT`/`TAKE_PROFIT_PERCENT` = 5% /
  7.5% via `risk_rules.compute_exit_prices`), so it's not naked — but 5%
  down on a $100k position is a **-$5,013** move, 7.5% up is **+$7,520**.
  Real dollar swings either way, not a rounding error.
- **Today's actual capital drought, confirmed independently in two places**:
  `strategy_manager`'s bracket-order path logged `Position size for AMD is
  0 (capital=$19.66, price=$501.00), skipping order` three times between
  13:34–13:36 ET today, and separately `ib_side_channel_trader`'s
  extended-hours path logged `Skipping extended-hours {AAPL,MSFT,NVDA,AMD,
  AMZN}: insufficient buying power this cycle` roughly 15 times between
  12:42–13:29 ET — two independent code paths agreeing the account is
  effectively out of buying power right now. Yesterday evening (9/8 19:2x
  ET) it was already down to $43.53–$43.54.
- `context_2026-09-09.json`'s `scalping_v2` signals on AMD/MSFT/NVDA/AAPL/
  AMZN this morning are all logged `"confirmed": 1` — real, secondary-
  indicator-confirmed entries per [[project_scalping_risk_tuning...]]-era
  logic — but per [[project_fractional_order_and_wash_trade_bugs]]'s
  standing warning, "confirmed"/"fired" in `signals.db` is never proof an
  order executed. Cross-referencing `bot.log`, essentially none of them
  did: they were confirmed by the strategy's own gate, then silently
  dropped for lack of capital one layer down.
- `strategy_toggles` in `signals.db` right now: `pod_stock=1, pod_etf=1`,
  `pod_commodity=0, pod_forex=0, pod_crypto=0` — so pod_stock is live and
  on, not "seeded OFF by default" as `CLAUDE.md` describes it (that's
  presumably just stale relative to whatever toggle change happened after
  it was last verified).

**What I did *not* do**: verify the account's real current net-liq/buying
power directly. That requires either the dashboard or a broker-side check,
and per the single-IB-login constraint, an independent check from this
session risks disconnecting the live bot's own Gateway session — I'd
rather flag the discrepancy and let you check it the way you already trust
(the dashboard) than risk that.

**Two different explanations, and I can't tell which from what's on disk**:

1. **This account genuinely has ~$990k in it** (plausible if it's a fresh/
   default IB paper account nobody resized down), in which case a single
   10%-of-portfolio position is working exactly as `portfolio_manager.py`
   and `config.MAX_POSITION_SIZE` are written to do — but it's a much
   bigger single-name concentration than anything else in this project's
   risk posture (the whole 8-core-strategy design, the small-account
   research, the Phase-5 $136 cap) has ever assumed, and it just quietly
   consumed the account's ability to fund every other strategy for over a
   day.
2. **The account is much smaller than that** (consistent with every other
   $19–800 figure logged elsewhere this week, and with
   [[project_ib_currency_and_scanner_findings_2026_09_09]]'s "real numbers
   are GBP not USD, ~low thousands" framing), and `portfolio_manager.py`
   is reading/computing `portfolio_value` from something inflated —
   possibly IB's underlying paper-account default rather than whatever
   real funded amount this account is supposed to represent, or a
   currency-unit mismatch (GBP vs USD). If so, that single TSLA fill is a
   real bug that put a wildly oversized position on a small account.

**Confidence**: high on the facts (the fill happened, the sizing math, the
resulting capital drought — all direct log evidence, cross-checked two
independent ways). Low on which of the two explanations above is true —
that's a factual question about the real account balance that only you (or
the dashboard) can answer safely from here.

**Risk of acting on this incorrectly**: if explanation (1) is right and
someone "fixes" this as a bug, you'd be capping a legitimate ~$990k
account down to toy position sizes. If explanation (2) is right and
nothing changes, `pod_stock` (and `pod_etf`, which is also on) will keep
sizing new positions off a portfolio value that isn't real, and could
place another oversized fill the next time a STOCK candidate passes.
Given the asymmetry — one path is "you unnecessarily shrink good position
sizing," the other is "another six-figure-notional order goes out on what
might be a much smaller real account" — I'd check the real number before
the next `pod_stock`/`pod_etf` cycle finds a fillable candidate.

**If you want a quick code-level safety net while you investigate**
(not applying this, just describing it — happy to also propose it as a
smaller, separate diff another day if you want it): add a hard dollar
ceiling alongside the existing percentage-of-portfolio one in
`portfolio_manager.py`'s `position_ceiling` calculation, e.g.:

```python
# portfolio_manager.py, near line 130/156
position_ceiling = min(
    portfolio_value * MAX_POSITION_PCT_OF_PORTFOLIO,
    MAX_POSITION_DOLLAR_CEILING,  # new constant, e.g. a few thousand
)
```

That would bound the blast radius of a bad `portfolio_value` read (or a
genuinely large account) without needing to resolve which of the two
explanations above is correct first.

---

## No trade proposal today

Every real signal I could see today (`scalping_v2` on AAPL/MSFT/NVDA/AMD/
AMZN, all confirmed) was already blocked by the account's own buying-power
shortage before I ever looked at it — proposing one of them would mean
recommending a trade the system itself can't currently fund. `momentum`'s
repeated CRM signal (7:51am–8:53am ET, same price band each cycle) never
confirmed either (`volume_confirmed: false` in the one feature row I
checked) — expected behavior for a daily-bar strategy re-logging a stable
decision every 60s cycle, not a new opportunity, and also not fundable
right now regardless.

Once the capital picture above is resolved, tomorrow's cycle should have
real room to act on.

# Group 2 Daily Proposal — 2026-09-08

## Summary

No trade proposals today. `context_2026-09-08.json` shows zero signals in the
last 24h, and `context_2026-09-07.json` (yesterday, never actually reviewed —
the cron's `claude` binary wasn't on PATH until today's fix) also shows zero.
Investigating why turned up something more important than any single trade:
**the live bot appears to have been down since 2026-09-05, ~00:22 UTC**
(2026-09-04 ~8:22pm ET) — over 3 trading days with no live signals generated
at all. That's the real reason both days are quiet, not a lack of market
opportunity.

I'm not proposing a trade today. I'm flagging the outage as the thing that
needs a human decision before tomorrow's cycle can produce anything real.

---

## Finding: live bot appears down since 2026-09-05

**What I checked** (all read-only, no state changed):

- `signals.db`: last real signal is `2026-09-04T23:31:06` (an
  `extended_hours_reconciled_on_startup` entry). Nothing since.
- `bot.log` (205,908 lines) ends mid-action at `2026-09-05 00:22:08`, on the
  line `Timeout: 3 positions still open` — right after a `/api/stop`-triggered
  graceful liquidation at `00:21:38` that closed CMOD, LOV, and ICOM via the
  IB aux channel but timed out with 3 positions still open. No log line
  exists after that timestamp, ever, in this file.
- `cron_launch.log` shows `start_bot_cron.sh` believes it successfully
  launched a `lumibot` tmux session on 2026-09-07 and again today
  (2026-09-08) at 09:25 ET.
- But `tmux ls` right now shows only `claude` and `ib_gateway` sessions — no
  `lumibot` session exists. Either it's crashing immediately after launch
  (tmux drops a session when its command exits) or something else killed it,
  but either way nothing from it reached `bot.log`.
- `ps aux` shows no `bot_runner.py` or `api.py` process running at all.
  `curl localhost:5000/api/status` gets nothing back — the dashboard API
  isn't listening.
- `watchdog.log` has been repeatedly restarting IB Gateway itself (down/up
  cycles on 9/5, 9/7 x2) but every entry ends with "Restart complete —
  trading NOT auto-resumed, needs an explicit Start" — consistent with
  watchdog only handling the *IB Gateway* process, not diagnosing why
  `bot_runner.py` itself isn't staying up.
- IB Gateway itself looks healthy right now — port 4002 is open, and the
  IBC log shows a clean simulated-trading login as recently as 09-08 04:18
  UTC.
- `bot_runner.py` compiles cleanly (`py_compile`, no syntax error), and
  `.env` has all the expected keys present (values not inspected). So it's
  not an obvious syntax break or missing credential file.
- One thing worth knowing before anyone restarts it: `git status` shows
  **13 tracked files modified and uncommitted** (including `bot_runner.py`
  itself — a 236-line uncommitted diff — plus `strategy_manager.py`,
  `api.py`, `config.py`, all 6 shared-universe strategies, and
  `requirements.txt`), one file deleted (`advanced_strategies.py`), and a
  large batch of new untracked modules (`order_executor.py`,
  `portfolio_manager.py`, `risk_rules.py`, `ib_connector.py`,
  `symbol_screener.py`, `signal_logger.py`, the 5 pod scanners, etc.). I
  didn't try to run the bot myself to reproduce the crash — starting/
  stopping the live bot needs your explicit go-ahead, not mine, per this
  project's own standing rule, and given how much is uncommitted right now
  I'd rather you see this list before anything gets restarted than have me
  guess at it.

**What I'd suggest, in order**: (1) decide whether the uncommitted changes
are finished work that should be committed, or a work-in-progress state that
explains the crash — if `bot_runner.py`'s own diff is mid-edit, that's the
first suspect; (2) try starting `bot_runner.py` manually in the foreground
(not via cron/tmux -d) so any startup exception actually prints somewhere
visible instead of vanishing with the tmux session; (3) once it's running
again, confirm the 3 positions that timed out mid-liquidation on 9/5
(CMOD, LOV, ICOM) are in the state you expect — 3+ days is long enough that
I wouldn't assume anything about them without checking the broker/dashboard
directly.

---

## No trade proposals today

Nothing in `signals.db` from the last 24h (or, for practical purposes, the
last 3+ days) to evaluate. I'd rather report that plainly than manufacture a
speculative pick with no real signal behind it. Once the bot is back up and
generating signals again, tomorrow's cycle should have real data to work
from.

**Confidence**: high, on the outage itself — this is a direct read of logs,
process state, and file timestamps, not an inference. Low/none on *why*
`bot_runner.py` won't stay up — I stopped short of running it to avoid
touching live bot state without sign-off, so the root cause is still open.

**Risk of acting on this**: none from me — this is a status report, not a
trade. The risk is entirely on the *inaction* side: every day this stays
down is a day with zero data for this comparison and (per `bot_runner.py`'s
own header) an IB paper account sitting with whatever position/order state
it was left in on 9/5.
